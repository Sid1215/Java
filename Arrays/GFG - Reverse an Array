class Solution {
    public void reverseArray(int arr[]) {
        // code here
        int [] temp = arr.clone();
        
        for(int i=temp.length-1; i>=0; i--)
        {
            arr[(arr.length-1)-i] =temp[i];
        }
     
    }
}
