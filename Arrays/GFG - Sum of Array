
class Solution {
    int arraySum(int arr[]) {
        // code here
        int temp=0;
        for(int i=0; i<arr.length; i++)
        {
            temp += arr[i];
        }
        return temp;
    }
}
